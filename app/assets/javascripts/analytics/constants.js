AKUMAI.namespace('AKUMAI.analytics.Constants');

AKUMAI.analytics.Constants = function(){
  var that = {};
  that.SVG_WIDTH = 200;
  that.SVG_HEIGHT = 200;

  Constants = function(){
    return that;
  };

  return that;
};